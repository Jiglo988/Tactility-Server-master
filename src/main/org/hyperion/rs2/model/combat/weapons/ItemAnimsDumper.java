package org.hyperion.rs2.model.combat.weapons;

public class ItemAnimsDumper {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
	    /*int id = 13879;
		String name = "morrigan's javelin";
		int standid = WeaponAnimManager.getStandAnimation(id);
		int walkid = WeaponAnimManager.getWalkAnimation(id);
		int defid = WeaponAnimManager.getDefendAnimation(id, Integer.MAX_VALUE);
		int runid = WeaponAnimManager.getRunAnimation(id);
		int atkid = WeaponAnimManager.getAttackAnimation(id, 1);
		//(int standId, int walkId, int runId, int attackId, int defendId)
		System.out.println("Id: " + id + "," + name);
		System.out.println("new WeaponAnimations(" + standid + ", " + walkid + ", " + runid + ", " + atkid + " ," + defid + ");");
		int speed = WeaponAnimManager.getSpeed(name, 4151);
		System.out.println("Speed: " + speed);*/
	}

}
